rules:
  - name: "Test"
    targets: files
    locations:
      # - path: ~/Desktop
      #   max_depth: 3
      - path: zip:///Users/thomasfeldmann/Downloads/30067_NL-AB-BBBC_SkywirePythonGPS.zip
        max_depth: null
    filters:
      - extension: pdf
      - filesize: "<1MB"
      - mimetype
      - filecontent: "(?P<all>.*)"
    actions:
      - echo: "{fs} - {fs_path} - {path} - {relative_path}"
      - echo: "{filesize}"
      - echo: "{mimetype}"
      - echo: "{filecontent}"

  # - name: Find some folders
  #   targets: dirs
  #   locations:
  #     - ~/Desktop
  #     - path: ~/Desktop/Inbox
  #       max_depth: null
  #   filters:
  #     - extension: pdf
  #   actions:
  #     - copy: ~/Dir

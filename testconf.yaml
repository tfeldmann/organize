rules:
  - name: "Find the last modification date of some png files"
    targets: dirs
    locations:
      - path: ~/Desktop/
        max_depth: 2
    filters:
      - created
      - regex: ^.*
      - name: "{test} {test2}"
    actions:
      - echo: "{name}"

  # - name: Find some folders
  #   targets: dirs
  #   locations:
  #     - ~/Desktop
  #     - path: ~/Desktop/Inbox
  #       max_depth: null
  #   filters:
  #     - extension: pdf
  #   actions:
  #     - copy: ~/Dir

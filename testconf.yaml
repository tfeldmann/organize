rules:
  - name: "Find the last modification date of some png files"
    targets: files
    locations:
      - path: ~/Desktop/Testfolder
        max_depth: null
      - path: ~/Desktop/Testfolder
        max_depth: null
      - path: "~/Desktop/Testfolder 2"
        max_depth: null
      - filesystem: zip:///Users/thomasfeldmann/Desktop/Testfolder.zip
        max_depth: null
        path: "/"
    filters:
      - duplicate
    actions:
      - echo: "{fs_path}"

  # - name: Find some folders
  #   targets: dirs
  #   locations:
  #     - ~/Desktop
  #     - path: ~/Desktop/Inbox
  #       max_depth: null
  #   filters:
  #     - extension: pdf
  #   actions:
  #     - copy: ~/Dir

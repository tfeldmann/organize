rules:
  - name: "Find the last modification date of some png files"
    targets: dirs
    locations:
      - path: ~/Desktop/test
        max_depth: null
    filters:
      - empty
    actions:
      - echo: "{path}"
      - delete

  # - name: Find some folders
  #   targets: dirs
  #   locations:
  #     - ~/Desktop
  #     - path: ~/Desktop/Inbox
  #       max_depth: null
  #   filters:
  #     - extension: pdf
  #   actions:
  #     - copy: ~/Dir

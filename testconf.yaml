rules:
  - name: "Find the last modification date of some png files"
    targets: files
    locations:
      - path: ~/Desktop/test
        max_depth: 3
    filters:
      - hash:
          algorithm: md5
    actions:
      - echo: "{hash}"

  # - name: Find some folders
  #   targets: dirs
  #   locations:
  #     - ~/Desktop
  #     - path: ~/Desktop/Inbox
  #       max_depth: null
  #   filters:
  #     - extension: pdf
  #   actions:
  #     - copy: ~/Dir

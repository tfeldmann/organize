rules:
  - name: "Find the last modification date of some png files"
    targets: files
    locations:
      - path: ~/Desktop/test
        max_depth: 3
    filters:
      - name: "Bildschirmfoto {date} um {time}_*"
      - extension: png
    actions:
      - move: "~/Desktop/Fotos/{name}.png"

  # - name: Find some folders
  #   targets: dirs
  #   locations:
  #     - ~/Desktop
  #     - path: ~/Desktop/Inbox
  #       max_depth: null
  #   filters:
  #     - extension: pdf
  #   actions:
  #     - copy: ~/Dir

rules:
  - name: "Find the last modification date of some png files"
    targets: dirs
    locations:
      - path: ~/Desktop/test
        max_depth: 0
    filters:
      - name: "more"
    actions:
      - echo: "{name}"
      - copy:
          dest: ~/Desktop/test/ahoi/
          on_conflict: "skip"
          rename_template: "{name} -- {counter}"

  # - name: Find some folders
  #   targets: dirs
  #   locations:
  #     - ~/Desktop
  #     - path: ~/Desktop/Inbox
  #       max_depth: null
  #   filters:
  #     - extension: pdf
  #   actions:
  #     - copy: ~/Dir

sudo: false
language: python
python:
  - "3.4"
  - "3.5"
  - "3.6"
install:
  - pip install poetry
  - poetry check
  - poetry install
  - poetry run python main.py list
  - poetry run python main.py --version
script:
    poetry run py.test
